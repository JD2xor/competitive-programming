#include <bits/stdc++.h>
using namespace std;

using LL = long long;

int main() {
  cin.tie(0);
  ios_base::sync_with_stdio(false);

  int N;
  cin >> N;

  if (N < 1000) {
    cout << "ABC" << '\n';
  } else {
    cout << "ABD" << '\n';
  }

  return 0;
}